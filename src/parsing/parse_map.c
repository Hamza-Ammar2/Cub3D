#include "cub3d.h"

/*
** Read the map block (must be the last element of the file) into a char **.
** Preserve spaces exactly as they appear; do not trim the grid.
*/
int	parse_map(t_game *game, int fd)
{
	(void)game;
	(void)fd;
	return (0);
}
