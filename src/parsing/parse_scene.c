#include "cub3d.h"

/*
** Entry point for parsing the .cub file.
** Responsibilities: check extension, open file, dispatch each line to the
** texture / color / map parsers, then run validate_map().
*/
int	parse_scene(t_game *game, char *path)
{
	(void)game;
	(void)path;
	return (0);
}
