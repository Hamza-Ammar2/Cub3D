#include "cub3d.h"

/*
** Validate the parsed map: only allowed chars (0 1 N S E W space), exactly one
** player start, and fully closed by walls (flood-fill or edge/neighbour check).
*/
int	validate_map(t_game *game)
{
	(void)game;
	return (0);
}
